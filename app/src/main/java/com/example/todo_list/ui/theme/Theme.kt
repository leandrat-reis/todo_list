package com.example.todoapp.ui.theme

import androidx.compose.ui.graphics.Color

// Cores para o tema claro
val LightSecondary = Color(0xFF03DAC6)
val LightTertiary = Color(0xFFBB86FC)
val LightBackground = Color(0xFFFFFFFF)
val LightSurface = Color(0xFFFFFFFF)      // Branco puro
val LightOnPrimary = Color.White          // Texto branco sobre primary
val LightOnSecondary = Color.Black        // Texto preto sobre secondary
val LightOnBackground = Color(0xFF1C1B1F) // Texto escuro
val LightOnSurface = Color(0xFF1C1B1F)    // Texto escuro
val LightError = Color(0xFFB00020)        // Vermelho para erros
val LightOnError = Color.White            // Texto branco sobre erros

// Cores para o tema escuro
val DarkPrimary = Color(0xFFBB86FC)       // Um roxo mais claro (para contraste no escuro)
val DarkSecondary = Color(0xFF03DAC6)      // Um verde água claro
val DarkTertiary = Color(0xFF3700B3)      // Um roxo escuro
val DarkBackground = Color(0xFF121212)    // Quase preto
val DarkSurface = Color(0xFF121212)       // Quase preto
val DarkOnPrimary = Color.Black           // Texto preto sobre primary
val DarkOnSecondary = Color.Black         // Texto preto sobre secondary
val DarkOnBackground = Color(0xFFE0E0E0)  // Texto claro
val DarkOnSurface = Color(0xFFE0E0E0)     // Texto claro
val DarkError = Color(0xFFCF6679)         // Vermelho para erros
val DarkOnError = Color.Black             // Texto preto sobre erros

// Cores para as tarefas (exemplo, você pode ajustar)
val TaskCompletedColor = Color(0xFF4CAF50) // Verde para tarefas concluídas
val TaskPendingColor = Color(0xFFFFC107)  // Amarelo para tarefas pendentes
